import { Lexend_Deca, Tenor_Sans } from 'next/font/google';

export const lexendDeca = Lexend_Deca({ subsets: ['latin'] });

export const tenorSans = Tenor_Sans({ subsets: ['latin'], weight: ['400'] });
